# KNN_CLASSIFICATION_
K-Nearest Neighbors (KNN) is a simple supervised machine learning algorithm used for classification. It predicts the class of a new data point by finding the nearest training data points and using majority voting. This project covers data preprocessing, choosing K, model training, prediction, and evaluation.
